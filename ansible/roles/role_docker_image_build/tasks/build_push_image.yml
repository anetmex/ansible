- name: build image
  docker_image:
    path: /home/deploy/
    name: "simple-rest-app_{{ version }}"
    tag: latest
    push: yes
    repository: "{{ registry_host }}:{{ registry_port }}"
    source: build
    state: present