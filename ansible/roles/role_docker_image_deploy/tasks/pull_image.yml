- name: pull image from registry
  docker_image:
    build:
      path: /home/deploy/
    name: "{{ registry_host }}:{{ registry_port }}/simple-rest-app_{{ version }}"
    tag: latest
    pull: yes
    #repository: "{{ registry_host }}:{{ registry_port }}"
    source: pull
    state: present